<?xml version="1.0" encoding="utf-8"?>
<!--
    Network security configuration for MediMind.

    The app is fully offline — no remote API or network call is made.
    This config enforces HTTPS-only in release builds and allows cleartext
    in debug builds for local proxy tools (Charles, Fiddler, etc.).

    REFERENCED FROM AndroidManifest.xml:
      android:networkSecurityConfig="@xml/network_security_config"
-->
<network-security-config>
    <!-- Production: standard HTTPS only, no cleartext traffic permitted -->
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>

    <!-- Debug: allow cleartext and user-installed certificates (for proxying) -->
    <debug-overrides>
        <trust-anchors>
            <certificates src="system" />
            <certificates src="user" />
        </trust-anchors>
    </debug-overrides>
</network-security-config>
